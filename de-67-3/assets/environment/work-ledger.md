# DE-67 work ledger

DFS: `.de67/DFS.md`
Lineage: `<one stable project identity; the deadline database binds it on first task>`

Keep only the current batch. The active list contains at most ten still-red DFS claims. Each item is
free-form prose beneath an unchecked claim line. Keep only its present causal frontier, active route,
and current clock task id after dispatch. A worker finding keeps its claim active, but its replacement
route overwrites the old attempt notes. Delete an item as soon as its DFS claim is accepted. Do not
keep a batch result, archive, completion summary, transcript, or chronological task history; the
checked DFS claim is the complete durable record.

## Active work

<!-- Example: - [ ] R-001 — Make the named owner perform the DFS transition. -->
