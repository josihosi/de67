interface:
  display_name: "DE67 Specification Coordinator"
  short_description: "Two-gate xhigh specification coordination"
  default_prompt: "Use $de67 to prepare a brief, ask before an xhigh author creates the specification, then ask again before a coordinator dispatches atomic work."

policy:
  allow_implicit_invocation: false
