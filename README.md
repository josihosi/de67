# de67

Sometimes things are 67.
When things are 67, you need to de67 them.

`$de67-lab : B -> consent_S -> Q_S{xhigh}:S_frozen -> consent_C -> Q_C{xhigh}:L{necessary, open<=10} -> W_i -> G -> next(L) | halt`

`Q_C != W_i`; execution returns compact `J_i`, accepted proof advances the product frontier, and qualifying failure maps `P_n -> P_n+1` through an isolated serial benchmark while `K` and `S_core` remain fixed.

This repository is a stable checkpoint. Active evolution happens in [josihosi/de67-lab](https://github.com/josihosi/de67-lab).
