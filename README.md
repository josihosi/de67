# de67

Sometimes things are 67.
When things are 67, you need to de67 them.

`$de67` : request -> `S=(C,E,D,O,A,R)` -> consent -> `Q{xhigh}` -> `L{atomic,open<=10}` -> `W_i` -> `G` -> halt.
`F{Luna|Terra}` holds dialogue and specification; `Q` freezes truth and routes the weakest sufficient worker; `G` accepts only an identity-matched, authoritative, non-manufactured proof.
