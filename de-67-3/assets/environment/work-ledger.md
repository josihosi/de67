# DE-67 work ledger

DFS: `.de67/DFS.md`
Lineage: `<one stable project identity; the deadline database binds it on first task>`

Keep only the current batch. The active list contains at most ten still-red DFS claims. Each item is
free-form prose beneath an unchecked claim line; useful details are outcome, code scope, worker,
passing test, dependencies, estimate/deadline, and current result. Retain the exact clock task id
from dispatch until DFS promotion so a recorded completion can be reconciled after interruption. A
worker finding keeps its claim active. Record the dependency or replacement route concisely; newly
discovered DFS claims enter only when necessary and without exceeding the active ceiling. Delete an
item as soon as its DFS claim is
accepted. Do not keep a batch result, archive, or completion summary; the checked DFS claim is the
complete durable record. For a still-red claim, replace prior attempt notes with only the current
causal frontier and active route. Do not append a chronological task history.

## Active work

<!-- Example: - [ ] R-001 — Make the named owner perform the DFS transition. -->
