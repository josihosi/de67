interface:
  display_name: "DE67 Specification Coordinator"
  short_description: "Artifact-first specification coordination"
  default_prompt: "Use $de67 to prepare a brief, ask before an xhigh author freezes the specification, then ask again before an xhigh coordinator assigns rolling atomic work to workers and gates compact evidence."

policy:
  allow_implicit_invocation: false
