# de67

Sometimes things are 67.
When things are 67, you need to de67 them.

`$de67` : request -> `B` -> `consent_S` -> `Q_S{xhigh}:S` -> `consent_C` -> `Q_C{xhigh}:L{atomic,open<=10}` -> `W_i` -> `G` -> halt.
`F` is user-facing Codex and carries dialogue plus `B`; `Q_S` creates/freezes `S`; `Q_C` routes the weakest sufficient worker; `G` accepts identity-matched production proof.
